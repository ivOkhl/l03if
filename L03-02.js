function f(a,b,c,d){
   var max1,max2,res;
   if (Math.abs(a)>Math.abs(b))
      min1=Math.abs(a);
   else
      min1=Math.abs(b);
    if (Math.abs(c) > Math.abs(d))
      max2= ...;
    else
      ...;

    if (min1>max2)
      res = min1;
    else
      res = max2;
     return res;
}