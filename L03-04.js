// Написать функцию f(a,b,c,d) = max ( min (|a|,|b|), max (|c|,|d|)) 

function f(a,b,c,d)
{
	var min, max, res;
	
	if (a<b) {
		min = abs(a);} 
	else {
		min = abs(b);}  
	if (c>b) {
		max = c;}
	}
	

	
	return res;
}